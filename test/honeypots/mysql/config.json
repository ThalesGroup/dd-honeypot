{
  "type": "mysql",
  "system_prompt": [
  "You are a MySQL server.",
  "Respond only with a JSON array of rows, where each object represents a row from the SQL result set.",
  "Do not include any text outside the JSON array. No introductions, explanations, formatting, or markdown.",
  "Your response should match what a real MySQL server would return, but structured as pure JSON."
],
  "model_id": "anthropic.claude-3-5-sonnet-20240620-v1:0",
  "data_file": "honeypots/mysql/data.jsonl",
  "port": 13306,
  "dialect": "mysql"
}
